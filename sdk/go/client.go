// Package agentsdk is the agent-side client: obtain a passport, hold the
// private key, sign requests per AIP-1, and surface AI-Budget-Remaining so an
// agent can self-throttle rather than hitting the wall.
package agentsdk
